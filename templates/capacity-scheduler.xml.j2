<?xml version="1.0"?>

<!--
{{ ansible_managed|comment }}
-->

<configuration>
{% for entry in (hadoop_properties['capacity-scheduler'] | dict2items)  %}
    <property>
        <name>{{ entry.key }}</name>
        <value>{{ entry.value }}</value>
    </property>
{% endfor %}
</configuration>
