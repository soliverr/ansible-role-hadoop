<?xml version="1.0"?>

<!--
{{ ansible_managed|comment }}
-->

<configuration>
{% for entry in (hadoop_properties['yarn-site'] | dict2items)  %}
    <property>
        <name>{{ entry.key }}</name>
        <value>{{ entry.value }}</value>
    </property>
{% endfor %}
</configuration>
